name: jetex_app
description: Jetex, Your online shopping assistant

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter



  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.0
  provider: ^5.0.0
  shared_preferences: ^2.0.5
  flutter_svg: ^0.19.0
  flutter_circular_chart: ^0.1.0
  dotted_border: ^1.0.7
  expansion_card: ^0.1.0
  google_maps_flutter: ^2.0.1
  geolocator: ^7.0.1
  geocoding: ^2.0.0
  share: ^2.0.1
  credit_card_type_detector: ^1.1.0
  image_picker: ^0.7.4
  flutter_local_notifications: ^5.0.0
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
     - assets/ui/
     - assets/ui/icons/
     - assets/ui/icons/warehouse.svg
     - assets/ui/icons/cargo.svg
     - assets/ui/icons/arrived.svg
     - assets/ui/icons/referral.svg
     - assets/ui/logos/flo_logo.svg
     - assets/ui/icons/logout.svg
     - assets/ui/logos/boyner_logo.svg
     - assets/ui/logos/jetex_logo.svg
     - assets/ui/logos/visa_logo.svg
     - assets/ui/logos/mastercard_logo.svg
     - assets/ui/icons/order_box.svg
     - assets/ui/icons/declare_icon.svg
     - i18n/en.json
     - i18n/tr.json
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family:  CustomIcons
      fonts:
        - asset: assets/fonts/CustomIcons.ttf
    - family: HelveticaNeue
      fonts:
        - asset: assets/fonts/HelveticaNeue/HelveticaNeue-UltraLight.otf
          weight: 100
        - asset: assets/fonts/HelveticaNeue/HelveticaNeue-Thin.ttf
          weight: 200
        - asset: assets/fonts/HelveticaNeue/HelveticaNeue-Light.ttf
          weight: 300
        - asset: assets/fonts/HelveticaNeue/HelveticaNeue-Regular.ttf
          weight: 400
        - asset: assets/fonts/HelveticaNeue/HelveticaNeue-Medium.ttf
          weight: 500
        - asset: assets/fonts/HelveticaNeue/HelveticaNeue-Bold.ttf
          weight: 700
        - asset: assets/fonts/HelveticaNeue/HelveticaNeue-CondensedBold.ttf
          weight: 800
        - asset: assets/fonts/HelveticaNeue/HelveticaNeue-CondensedBlack.ttf
          weight: 900

  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
